N = int(input())
list_name = {}

for i in range(1, N+1):
    name = input().split()
    list_name[name[0]] = name[1]

M = int(input())
for i in range(M):
    name = input()
    res = (''.join({key for key, value in list_name.items() if name == key or name == value}))
    print('Not Found' if len(res) == 0 else res)
